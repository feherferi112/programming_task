namespace prog_tasks.Tasks.Task_4
{
    internal class CoordsUnsorted
    {
        /*
         If the coordinates in the WellTrack object are not sorted
        1. Sort:
        - define an origo vec0 = (0,0,0)
        - loop through the WellTrack object's coords
        - calculate the distance from vec0 for every point
        - sort them according to the distances 

        2. Calculate the distance with GetDistanceAlongTrack method writen in CoordsSorted.cs
         */
    }
}
